# Tax Engine

Taxes map to specific products and regions using `tax_rules` joining to `tax_codes`.
It supports compound calculations for specific zones (e.g. Sales Tax + VAT logic depending on region).
