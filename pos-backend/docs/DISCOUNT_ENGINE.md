# Discount Engine

The Discount Engine applies real-time Cart level and Item level manual overrides during checkout.

It operates similarly to the Promotion Engine but requires `userId` approval bounds to ensure cashiers do not exceed their authorized `Maximum Allowed Discount` permissions.
