# Warehouse Management

## Hierarchy
Warehouses can be chained using `parentId` to form regional hubs connecting to smaller storefronts.

## Locations
Inside a warehouse, granular tracking is handled via `warehouse_locations` supporting types like `ZONE`, `AISLE`, `BIN`.
